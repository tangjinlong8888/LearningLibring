<!-- 首页中的导航切换组价 -->

<view class='container'>
  <image class="navi-icon navi-left"  bind:tap="onLeft"       
    src="{{highLeftSrc}}" />
  <text class="title">{{title}}</text>
  <image class="navi-icon navi-right" bind:tap="onRight"
    src="{{highRightSrc}}" />
</view>